放镜头（运镜 VMD）
==================

支持：镜头用的 .vmd（不是人物动作）
程序启动时也会在这里生成一批内置运镜，空着也能用。

自己加的话直接把镜头 vmd 放进来：

  cameras/
    环绕一周.vmd
    特写推进.vmd

文件名带「特写 / 环绕 / 推拉 / 定镜 / 电影」会自动归类。
不要提交进 git（内置生成的也不要提交）。

Put camera-motion .vmd files here. Built-in cameras are generated at runtime. Do not commit them.
